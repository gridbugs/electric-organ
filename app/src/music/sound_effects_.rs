use caw::prelude::*;

pub fn melee(trig: impl SigT<Item = bool>) -> SigBoxed<f32> {
    Sig(0.0).boxed()
}

pub fn death(trig: impl SigT<Item = bool>) -> SigBoxed<f32> {
    Sig(0.0).boxed()
}

pub fn pistol(trig: impl SigT<Item = bool>) -> SigBoxed<f32> {
    Sig(0.0).boxed()
}

pub fn shotgun(trig: impl SigT<Item = bool>) -> SigBoxed<f32> {
    Sig(0.0).boxed()
}

pub fn rocket(trig: impl SigT<Item = bool>) -> SigBoxed<f32> {
    Sig(0.0).boxed()
}

pub fn explosion(trig: impl SigT<Item = bool>) -> SigBoxed<f32> {
    Sig(0.0).boxed()
}
